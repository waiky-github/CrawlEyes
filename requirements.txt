# Extraction
crawl4ai>=0.9.0
playwright>=1.40.0
pydantic>=2.0.0

# Search + MCP server (crawl_search_standalone.py / mcp_crawl_server.py)
httpx>=0.27
numpy>=1.24

# MCP server (mcp_crawl_server.py) — use mcp 1.x for the stable FastMCP API
mcp>=1.0,<2

# Optional: semantic rerank (crawl_search_standalone.py rerank=True)
# fastembed (ONNX, no torch) + downloads BAAI/bge-small-zh-v1.5 on first use
fastembed>=0.3
